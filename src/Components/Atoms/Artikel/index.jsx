/* eslint-disable react/prop-types */
// eslint-disable-next-line no-unused-vars
import React from "react";

const Artikel = ({ title, img }) => {
  return (
  <p></p>
  );
};

export default Artikel;
